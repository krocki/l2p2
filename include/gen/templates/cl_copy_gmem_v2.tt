__kernel void k_gen_$N$_$L$_$W$_$I$_$T$$V$_cl_copy_gmem (
    __global $T$$V$ * restrict out,
    __global const $T$$V$ * restrict in) {

	const int gid = get_global_id (0);

#pragma unroll `$I$ / $V$`
	for (int i = 0; i < `$I$ / $V$`; i++) {
		out[gid + i*$G$] = in[gid + i*$G$];
	}
}