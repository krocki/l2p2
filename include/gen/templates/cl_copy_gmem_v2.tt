__kernel void k_gen_$N$_$L$_$G$_$W$_$I$_$TV$_cl_copy_gmem (
	__global $TV$ * restrict out,
	__global const $TV$ * restrict in) {

	#if $I$>0
	int gid = get_global_id (0);
	#endif

	#if $I$>1
	#pragma unroll $I$
	#endif
	#if $I$>1
	for (int i = 0; i < $I$; i++) {
	#endif
	#if $I$>0
		out[gid] = in[gid];
	#endif
	#if $I$>1
	gid += $G#;
	}
	#endif

}